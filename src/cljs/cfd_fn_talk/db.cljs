(ns cfd-fn-talk.db)

(def default-db
  {:name       "cfd-(fn)-talk"
   :categories #{:lazy/immutable
                 :lisp
                 :multicore
                 :divergent-thinking
                 :people-in-computing
                 :limits
                 :elixir/erlang
                 :messages>classes}})
